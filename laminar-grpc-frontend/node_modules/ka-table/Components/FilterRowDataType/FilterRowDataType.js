var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import React from 'react';
import { DataType } from '../../enums';
import FilterRowBoolean from '../FilterRowBoolean/FilterRowBoolean';
import FilterRowDate from '../FilterRowDate/FilterRowDate';
import FilterRowNumber from '../FilterRowNumber/FilterRowNumber';
import FilterRowString from '../FilterRowString/FilterRowString';
var FilterRowDataType = function (props) {
    switch (props.column.dataType) {
        case DataType.Boolean: return React.createElement(FilterRowBoolean, __assign({}, props));
        case DataType.Date: return React.createElement(FilterRowDate, __assign({}, props));
        case DataType.Number: return React.createElement(FilterRowNumber, __assign({}, props));
        default: return React.createElement(FilterRowString, __assign({}, props));
    }
};
export default FilterRowDataType;