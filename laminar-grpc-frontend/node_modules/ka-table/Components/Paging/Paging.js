var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import defaultOptions from '../../defaultOptions';
import PagingPages from '../PagingPages/PagingPages';
var Paging = function (props) {
    var enabled = props.enabled, pagesCount = props.pagesCount;
    if (enabled) {
        var pages = new Array(pagesCount).fill(undefined).map(function (_, index) { return index; });
        return (React.createElement("div", { className: defaultOptions.css.paging },
            React.createElement(PagingPages, __assign({}, props, { pages: pages }))));
    }
    return (React.createElement(React.Fragment, null));
};
export default Paging;