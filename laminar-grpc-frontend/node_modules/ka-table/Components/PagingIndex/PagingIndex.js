var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
import * as React from 'react';
import { updatePageIndex } from '../../actionCreators';
import defaultOptions from '../../defaultOptions';
import { getElementCustomization } from '../../Utils/ComponentUtils';
var PagingIndex = function (props) {
    var childComponents = props.childComponents, dispatch = props.dispatch, isActive = props.isActive, pageIndex = props.pageIndex, text = props.text;
    var _a = getElementCustomization({
        className: defaultOptions.css.pagingPageIndex + " " + (isActive ? 'ka-paging-page-index-active' : ''),
        onClick: function () { return dispatch(updatePageIndex(pageIndex)); }
    }, props, childComponents.pagingIndex), elementAttributes = _a.elementAttributes, content = _a.content;
    return (React.createElement("div", __assign({}, elementAttributes), content || text));
};
export default PagingIndex;